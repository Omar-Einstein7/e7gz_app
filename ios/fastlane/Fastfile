default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    # Match is run in the CI before this to fetch certs
    # In CI, we use pilot to upload the pre-built IPA
  end

  desc "Push a new release build to the App Store"
  lane :release do
    # App Store release logic
  end
end
